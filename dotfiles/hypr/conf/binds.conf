# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

#Binds for thinkpad keyboard
# SUPER key
$mainMod = SUPER

# Actions
bind = $mainMod, RETURN, exec, alacritty # Open Alactritty
bind = $mainMod, Q, killactive # Close current window
bind = $mainMod, M, exit # Exit Hyprland
bind = $mainMod, E, exec, ~/.config/ml4w/settings/filemanager.sh # Opens the filemanager
bind = $mainMod, T, togglefloating # Toggle between tiling and floating window
bind = $mainMod, F, fullscreen # Open the window in fullscreen
bind = $mainMod CTRL, RETURN, exec, rofi -show drun # Open rofi
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, B, exec, ~/.config/ml4w/settings/browser.sh # Opens the browser
bind = $mainMod SHIFT, B, exec, ~/.config/ml4w/scripts/reload-waybar.sh # Reload Waybar
bind = $mainMod SHIFT, W, exec, ~/.config/ml4w/scripts/reload-hyprpaper.sh # Reload hyprpaper after a changing the wallpaper

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l # Move focus left
bind = $mainMod, right, movefocus, r # Move focus right
bind = $mainMod, up, movefocus, u # Move focus up
bind = $mainMod, down, movefocus, d # Move focus down

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1 # Switch to workspace 1
bind = $mainMod, 2, workspace, 2 # Switch to workspace 2
bind = $mainMod, 3, workspace, 3 # Switch to workspace 3
bind = $mainMod, 4, workspace, 4 # Switch to workspace 4
bind = $mainMod, 5, workspace, 5 # Switch to workspace 5
bind = $mainMod, 6, workspace, 6 # Switch to workspace 6
bind = $mainMod, 7, workspace, 7 # Switch to workspace 7
bind = $mainMod, 8, workspace, 8 # Switch to workspace 8
bind = $mainMod, 9, workspace, 9 # Switch to workspace 9
bind = $mainMod, 0, workspace, 10 # Switch to workspace 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1 #  Move window to workspace 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2 #  Move window to workspace 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3 #  Move window to workspace 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4 #  Move window to workspace 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5 #  Move window to workspace 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6 #  Move window to workspace 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7 #  Move window to workspace 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8 #  Move window to workspace 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9 #  Move window to workspace 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 #  Move window to workspace 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1 # Scroll workspaces 
bind = $mainMod, mouse_up, workspace, e-1 # Scroll workspaces

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow # Move window
bindm = $mainMod, mouse:273, resizewindow # Resize window

#Brightness
bind = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bind = ,XF86MonBrightnessUp, exec, brightnessctl s +10%

bind = ,XF86AudioLowerVolume, exec, pamixer -d 10
bind = ,XF86AudioRaiseVolume, exec, pamixer -i 10




#Binds for Varuna Abnt2 keyboard
# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# SUPER key
$secondMod = XKB_KEY_Alt_L

# Actions
bind = $secondMod, RETURN, exec, alacritty # Open Alactritty
bind = $secondMod, Q, killactive # Close current window
bind = $secondMod, M, exit # Exit Hyprland
bind = $secondMod, E, exec, ~/.config/ml4w/settings/filemanager.sh # Opens the filemanager
bind = $secondMod, T, togglefloating # Toggle between tiling and floating window
bind = $secondMod, F, fullscreen # Open the window in fullscreen
bind = $secondMod CTRL, RETURN, exec, rofi -show drun # Open rofi
bind = $secondMod, P, pseudo, # dwindle
bind = $secondMod, J, togglesplit, # dwindle
bind = $secondMod, B, exec, ~/.config/ml4w/settings/browser.sh # Opens the browser
bind = $secondMod SHIFT, B, exec, ~/.config/ml4w/scripts/reload-waybar.sh # Reload Waybar
bind = $secondMod SHIFT, W, exec, ~/.config/ml4w/scripts/reload-hyprpaper.sh # Reload hyprpaper after a changing the wallpaper

# Move focus with mainMod + arrow keys
bind = $secondMod, left, movefocus, l # Move focus left
bind = $secondMod, right, movefocus, r # Move focus right
bind = $secondMod, up, movefocus, u # Move focus up
bind = $secondMod, down, movefocus, d # Move focus down

# Switch workspaces with mainMod + [0-9]
bind = $secondMod, 1, workspace, 1 # Switch to workspace 1
bind = $secondMod, 2, workspace, 2 # Switch to workspace 2
bind = $secondMod, 3, workspace, 3 # Switch to workspace 3
bind = $secondMod, 4, workspace, 4 # Switch to workspace 4
bind = $secondMod, 5, workspace, 5 # Switch to workspace 5
bind = $secondMod, 6, workspace, 6 # Switch to workspace 6
bind = $secondMod, 7, workspace, 7 # Switch to workspace 7
bind = $secondMod, 8, workspace, 8 # Switch to workspace 8
bind = $secondMod, 9, workspace, 9 # Switch to workspace 9
bind = $secondMod, 0, workspace, 10 # Switch to workspace 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $secondMod SHIFT, 1, movetoworkspace, 1 #  Move window to workspace 1
bind = $secondMod SHIFT, 2, movetoworkspace, 2 #  Move window to workspace 2
bind = $secondMod SHIFT, 3, movetoworkspace, 3 #  Move window to workspace 3
bind = $secondMod SHIFT, 4, movetoworkspace, 4 #  Move window to workspace 4
bind = $secondMod SHIFT, 5, movetoworkspace, 5 #  Move window to workspace 5
bind = $secondMod SHIFT, 6, movetoworkspace, 6 #  Move window to workspace 6
bind = $secondMod SHIFT, 7, movetoworkspace, 7 #  Move window to workspace 7
bind = $secondMod SHIFT, 8, movetoworkspace, 8 #  Move window to workspace 8
bind = $secondMod SHIFT, 9, movetoworkspace, 9 #  Move window to workspace 9
bind = $secondMod SHIFT, 0, movetoworkspace, 10 #  Move window to workspace 10

# Scroll through existing workspaces with mainMod + scroll
bind = $secondMod, mouse_down, workspace, e+1 # Scroll workspaces 
bind = $secondMod, mouse_up, workspace, e-1 # Scroll workspaces

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $secondMod, mouse:272, movewindow # Move window
bindm = $secondMod, mouse:273, resizewindow # Resize window